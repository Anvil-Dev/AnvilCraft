gradle.projectsEvaluated {
    rootProject.tasks.named('runClient') {
        if (!(it instanceof JavaExec)) throw new GradleException('Expected JavaExec for isolated validation arguments')
        systemProperty 'anvilcraft.portVisualScene', 'true'
        systemProperty 'anvilcraft.portCfaItemScene', 'true'
        systemProperty 'anvilcraft.portMunMusicScene', 'true'
        systemProperty 'anvilcraft.portMunSkyScene', 'false'
        systemProperty 'anvilcraft.portMunSurfaceScene', 'false'
    }
}
